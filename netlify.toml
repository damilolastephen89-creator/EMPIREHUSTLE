[build]
  # Adjust this if your site files are in a subfolder like "dist" or "public"
  publish = "/"

[[plugins]]
  package = "netlify-plugin-a11y"

  [plugins.inputs]
    checkPaths = ["/index.html"]
    resultMode = "warn"
    timeout = 60000

